import * as summary from './summary'
import config from 'selectors/config'
import * as users from './users'
import * as kafkaReader from './kafkaReader'
import * as kafkaWebSocket from './kafkaWebSocket'

export default {
    summary,
    config,
    users,
    kafkaReader,
    kafkaWebSocket,
}
